server:
  port: 80

spring:
  application:
    name: cloud-order-com.customer.springcloud.service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: cloud-consul-order-com.customer.springcloud.service